# 平台 API 调用层

本上下文统一描述业务应用通过 SDK 调用微信与支付宝官方平台时使用的领域词汇。

## 术语

**平台协议**：
官方平台对身份认证、报文格式、签名验签和凭证生命周期的约定。
_避免_：业务接口、平台业务

**出站 API 调用**：
业务应用依照平台协议向官方端点发送请求并接收响应的交互。
_避免_：本地操作、协议处理

**平台通道**：
共享配置、认证方式和报文协议的一组出站 API 调用。
_避免_：业务模块、业务客户端

**调用身份**：
一次出站 API 调用采用的凭证角色，例如默认应用、微信授权方、支付宝用户或支付宝代调用应用。
_避免_：Token、业务用户

**派生资源**：
由可信平台响应提供，并按来源平台通道继续访问的 HTTPS 资源。
_避免_：任意 URL、匿名下载

**协议外能力**：
不属于出站 API 调用的通知处理、消息加解密、授权页面、客户端调起和业务数据处理。
_避免_：API 调用、平台通道能力
