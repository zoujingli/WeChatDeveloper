## Agent skills

### Development workflow

修改 `src/`、`tests/`、README、`docs/`、Composer 配置或 CI 前，必须执行 `docs/agents/development.md` 中的 AI 开发流程。只有受影响的公开行为、信任边界、测试和文档全部得到处理，且适用门禁通过后，任务才算完成。

### Issue tracker

本仓库的需求、规格和开发任务统一使用 GitHub Issues 管理。详细规则见 `docs/agents/issue-tracker.md`。

### Triage labels

问题分流采用 Matt Pocock 技能定义的五种标准状态标签。标签映射见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文领域文档布局。领域文档使用规则见 `docs/agents/domain.md`。
