# 更新记录

本文件记录面向 SDK 使用者和维护者的重要变化。平台官方接口本身的字段变化仍以平台官方文档为准。

## 2.0.0（尚未发布）

2.0 是一次主版本重写，把项目从大量业务接口类收敛为可组合、可测试的协议层 SDK。升级前请完整阅读[迁移指南](docs/migration-2.0.md)。

### 公开 API 与模块边界

- 根 `Client` 提供六个显式类型工厂，并保留配置驱动的 `get()`；移除通过 `__call()` 猜测工厂名称的入口。
- 微信和支付宝客户端统一采用“官方 path/API method + 参数数组”的调用模型；只有新协议形态无法安全表达时才增加专用方法。
- 配置统一实现 `ConfigInterface`，缓存和服务平台授权方存储分别通过 `StoreCacheInterface`、`StoreTokenInterface` 注入。
- 2.0 不恢复 1.x 的旧命名空间、静态 `instance()`、全局 helper 或大量业务接口类。公开入口见[API 速查](docs/api.md)，职责边界见[设计](docs/design.md)。

### 安全默认值

- 微信支付和支付宝的商户/应用私钥、平台公钥及证书材料执行严格 RSA 与格式校验，非 RSA 密钥在配置阶段被拒绝。
- 配置字段校验后保持只读，`fromArray()` 拒绝非字符串凭证被隐式转换。
- 支付宝同步响应必须具有预期节点、标量业务 code 和有效平台签名；验签原始节点必须与最终解析节点一致，网关 JSON 编码和 AES 参数长度失败时显式报错。
- 微信支付普通 JSON 响应和错误响应在解析前使用原始 body 验签，并校验平台证书或公钥序列号。
- 微信支付通知按“验签 → 时间窗口 → JSON 解析 → resource 解密”的顺序处理，默认允许 300 秒偏差。
- `downloadBill()` 实现已验签元数据与 HTTPS 文件下载的两阶段流程，并拒绝重定向和不安全 URL。
- 微信通用平台客户端只接受相对 path，避免受信客户端成为任意 URL 请求器。细节见[配置与凭证](docs/configuration.md)、[微信支付](docs/payments.md)和[支付宝](docs/alipay.md)。

### 可靠性与诊断

- 所有 SDK 故障统一继承 `SdkException`，同时区分传输、平台 API 和签名错误，并通过 `context()` 提供诊断上下文。
- 缓存键按部署前缀、平台通道和逻辑键分段编码，段内点号转换为 `%2E`，同时满足 PSR-16 字符限制并消除跨段碰撞。
- `PsrSimpleCacheStore` 把后端异常和失败返回转换为 SDK 异常；生产刷新锁缺失时明确失败。
- `FileCacheStore` 使用临时文件和原子重命名发布新值，缓存文件权限收紧为 `0600`，并修复过期读取与并发写入交错时误删新值的问题。细节见[异常](docs/exceptions.md)和[缓存](docs/cache.md)。
- 服务平台授权方 token 在字段与有效期验证通过后才回写业务存储。

### 工程质量与文档

- CI 覆盖 PHP 8.1、8.2、8.3、8.4，并额外验证 PHP 8.1 最低依赖组合。
- Guzzle 安全基线提高到 `^7.15.3`，CI 增加锁定依赖安全审计。
- 发布流程复用完整质量门禁；带 `-alpha`、`-beta` 或 `-rc` 的标签创建预发布版本。
- PHPUnit 覆盖公开客户端、配置、异常、安全验签、缓存适配和文件缓存并发回归。
- README 收敛为快速入口，详细内容拆分为主题指南、[文档中心](docs/index.md)和[公开 API 速查](docs/api.md)。

## 1.x

1.x 的历史发布记录未在本文件追溯。2.0 稳定发布后，1.x 的必要维护应保留在独立维护分支。
